# GateLink
GateLink is a real-time IoT gate automation system built using ESP8266, FastAPI, and WebSockets. It enables secure remote gate control via voice commands (Siri) or API triggers with instant relay activation.
